- content_for(:title){ "Hall of Fame" }

%h2 Hall of Fame

%table
  %thead
    %td #
    %td
    %td Name
    %td Average score
  - @users.each do |user|
    %tr
      %td= "##{@points.index(user.points) + 1}"
      %td= image_tag(user.gravatar_url(:size => 20, :default => avatar_url), :class => 'gravatar')
      %td= link_to(user.login, user)
      %td= "#{sprintf("%.1f", user.average_score)}/5"
