%h2= "Submit your entry for “#{@contest.name}”"

- if logged_in?

  = form_for [@contest, @entry] do |f|

    = f.label :description
    = f.text_area :description

    = f.submit 'Submit your entry'

- else

  = link_to 'Log in via Github', '/auth/github'
